ctx (Context<InitializePool>)
│
├── accounts (InitializePool struct)
│   ├── payer (Signer)
│   │   ├── .key() → Pubkey của người trả phí
│   │   ├── .is_signer → true
│   │   └── .lamports() → Số SOL trong account
│   │
│   ├── token_a_mint (Account<Mint>)
│   │   ├── .key() → Pubkey của token A mint
│   │   ├── .supply → Tổng supply của token A
│   │   ├── .decimals → Số decimals (ví dụ: 6 cho USDC)
│   │   └── .mint_authority → Authority có thể mint thêm
│   │
│   ├── token_b_mint (Account<Mint>)
│   │   ├── .key() → Pubkey của token B mint
│   │   ├── .supply → Tổng supply của token B
│   │   ├── .decimals → Số decimals
│   │   └── .mint_authority → Authority có thể mint thêm
│   │
│   ├── pool_state (Account<PoolState>) [PDA]
│   │   ├── .key() → Pubkey của pool PDA
│   │   ├── .bump → u8 (chưa set, sẽ ghi sau)
│   │   ├── .token_a_mint → Pubkey (chưa set)
│   │   ├── .token_b_mint → Pubkey (chưa set)
│   │   ├── .token_a_vault → Pubkey (chưa set)
│   │   ├── .token_b_vault → Pubkey (chưa set)
│   │   ├── .fee_bps → u16 (chưa set)
│   │   ├── .current_tick → i32 (chưa set)
│   │   ├── .total_liquidity → u128 (chưa set)
│   │   └── .tick_spacing_bps → u16 (chưa set)
│   │
│   ├── token_a_vault (Account<TokenAccount>) [PDA]
│   │   ├── .key() → Pubkey của vault A
│   │   ├── .mint → Pubkey của token A mint
│   │   ├── .owner → pool_state.key() (authority)
│   │   ├── .amount → 0 (vault mới, chưa có tokens)
│   │   └── .delegate → None
│   │
│   ├── token_b_vault (Account<TokenAccount>) [PDA]
│   │   ├── .key() → Pubkey của vault B
│   │   ├── .mint → Pubkey của token B mint
│   │   ├── .owner → pool_state.key() (authority)
│   │   ├── .amount → 0 (vault mới, chưa có tokens)
│   │   └── .delegate → None
│   │
│   ├── token_program (Program<Token>)
│   │   └── .key() → TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
│   │
│   └── system_program (Program<System>)
│       └── .key() → 11111111111111111111111111111111
│
├── bumps (InitializePoolBumps) [Auto-generated by Anchor]
│   ├── pool_state → u8 (ví dụ: 254)
│   ├── token_a_vault → u8 (ví dụ: 255)
│   └── token_b_vault → u8 (ví dụ: 253)
│
├── program_id
│   └── Pubkey của concentrated_liquidity program
│
└── remaining_accounts
    └── Vec<AccountInfo> (rỗng trong trường hợp này)